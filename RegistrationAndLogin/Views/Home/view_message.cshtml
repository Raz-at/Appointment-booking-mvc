@model IEnumerable<RegistrationAndLogin.Models.message>

@{
    ViewBag.Title = "view_message";
    Layout = "~/Views/Shared/_LayoutHome.cshtml";
}
<br />
<h2>Details</h2>
<br />
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EmailID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.From)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.To)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Message1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EndDate)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {

        if (item.EmailID == ViewBag.email)
        {

            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.EmailID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.From)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.To)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Message1)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.EndDate)
                </td>
                <td>
                    @Html.ActionLink("Delete", "Delete_msg", new { id = item.Id })
                </td>
            </tr>
        }

        @*else if(item.EmailID == HttpContext.Current.User.Identity.Name)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.EmailID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.From)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.To)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Message1)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.EndDate)
                </td>
                <td>
                    @Html.ActionLink("edit", "edit", new { id = item.Id })
                </td>
            </tr>
        }*@
    }

</table>
<div>
    @Html.ActionLink("Back" , "Dashboard")
</div>
